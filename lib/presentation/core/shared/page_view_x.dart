import 'package:flutter/widgets.dart';

// TODO some how make page view able to get its page change logged

/**
 * extension PageViewX on PageView {
  void didNavigate(String oldPageName, String newPageName) {
    var currentPage = 0;

    final newPage = controller.page;

    if (newPage == 0 || currentPage == 1) {
      currentPage = newPage!.toInt();
    }
  }
}

 */