// CORE
import 'package:dart_counter/presentation/ios/core/core.dart';

class TrainingFlow extends StatelessWidget {
  const TrainingFlow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const AutoRouter();
  }
}
