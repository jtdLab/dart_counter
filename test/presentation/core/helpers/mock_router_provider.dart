import 'package:flutter/widgets.dart';

class MockRouterProvider extends StatelessWidget {
  const MockRouterProvider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement
    throw UnimplementedError();
  }
}
